# MyDay 产品需求文档（PRD v1）

> 状态：需求已确认，交互原型已完成（design/MyDay-prototype-v1.html），本文作为后续开发的唯一依据。
> 更新：2026-09-18

---

## 1. 产品目标和使用场景

### 1.1 一句话目标

给凯森自己用的一台电脑上、一个浏览器里的「每日个人主页」：打开就知道今天有什么课、天气怎么样、有什么要记的事。

### 1.2 使用场景

- 早上出门前：看一眼首页，知道今天第几节课、在哪栋楼、要不要带伞。
- 换周前：到课程页把下周的课表排好，或者把本周整周复制过去。
- 随手有事：想到什么立刻丢进备忘录，可能是几行笔记，也可能是个待办清单。
- 学期/作息变动：到设置页改第一周日期、节次时间、默认城市。
- 长期：数据永远在本地，不依赖任何线上账号，重装浏览器也不丢。

### 1.3 边界（本次确认）

- 只在 Windows 本机浏览器运行，地址形如 `http://127.0.0.1:<端口>`。
- 单用户，无登录、无账号体系。
- 除天气接口外不依赖网络；断网时除天气外全部功能正常。
- 不使用云数据库、不做部署、不做手机端适配。
- 数据必须落在 `D:\App`，刷新页面、关机重启后不丢。
- **零预置原则**：程序不内置任何个人信息——不预置凯森的真实课表、学校作息时间、校区、城市、姓名。这类内容一律由用户在设置中自行录入，首次启动时相关模块为空并显示引导。

---

## 2. 整体页面框架和导航结构

### 2.1 布局骨架

```
┌──────────┬────────────────────────────────────────────┐
│          │  顶栏：当前页标题 / 副标题      日期 · 第N周 │
│  侧边栏  ├────────────────────────────────────────────┤
│  首页    │                                            │
│  课程    │              内容区（单页切换）              │
│  天气    │                                            │
│  备忘录  │                                            │
│  数据与设置                                            │
└──────────┴────────────────────────────────────────────┘
```

- 侧边栏固定宽 208px，纵向五宫格导航，当前页高亮。
- 顶栏固定，右侧常驻「今天日期 + 当前第几教学周」。
- 五个页面为同级页签，不做二级路由，切换即时、不刷新。
- 视觉基调：浅色为主，白卡片 + 0.5px 描边 + 12px 圆角，主色蓝紫。

### 2.2 技术骨架（已定）

- 后端：本机 Python 标准库 HTTP 服务（不装第三方包），提供 REST 风格接口。
- 前端：单页 HTML + 原生 JS + CSS，无需构建工具。
- 通信：`fetch` 调本机接口，JSON 交换。
- 启动：双击一个启动脚本 → 起服务 → 自动打开默认浏览器。

### 2.3 接口约定（第一版）

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET / PUT | `/api/settings` | 读写设置 |
| GET | `/api/courses?week=N` | 取某一周课程；不带 week 返回全部 |
| POST / PUT / DELETE | `/api/courses` | 新增 / 修改 / 删除一节课 |
| POST | `/api/courses/copy` | 整周复制到目标周 |
| POST | `/api/import/preview` | 解析上传的课表 xlsx，只返回预览，不写盘 |
| POST | `/api/import` | 导入课表（先自动备份，再建节次、按周写课程） |
| GET / POST / PUT / DELETE | `/api/notes` | 备忘录增删改查 |
| POST | `/api/notes/toggle` | 只翻转某一条清单项的勾选（首页速览用，避免整段回写） |
| GET | `/api/weather` | 天气实况与预报（服务端转发，带缓存） |
| GET | `/api/cities?q=` | 按名字搜城市（Open-Meteo 地理编码） |
| GET / POST / DELETE | `/api/places` | 「我的地点」：列出 / 添加并设为当前 / 移除 |
| POST | `/api/places/select` | 切换当前城市 |
| GET | `/api/backup` | 导出备份 |
| POST | `/api/restore` | 从备份还原 |

---

## 3. 首页的内容结构

首页只做「汇总展示」，不承担录入，所有编辑跳到对应模块。

从上到下四块：

### 3.1 顶部状态行
- 问候语（按时间的早/午/晚上好）。
- 今天日期、星期几、第几教学周、校区。
- 右侧标签：今天是本周第 N 周、今天共 M 节课。

### 3.2 今日课程（占 2 列）
- 按节次时间正序排列今天的所有课。
- 每条显示：节次 + 起止时间、课程名、地点、备注/老师。
- 状态：已结束的课灰色置底；正在上的课高亮；未来最近一节标「下一节」。
- 今天没课时显示「今天没课，休息一下」。
- 右上角「去课程页」跳转。

### 3.3 今日天气（占 1 列）
- 当前温度、天气状况、城市名。
- 体感温度、湿度、风力风向、今日最高/最低。
- 一条生活提示（如昼夜温差、是否带伞）。
- 数据拉取失败时显示「暂时取不到天气」，不空白、不报错弹窗。
- 右上角「去天气页」跳转。

### 3.4 备忘录速览（通栏）
- 最近 3 条，置顶优先，按修改时间倒序。
- 每条显示标题、类型和摘要（笔记首行 / 清单完成数 x/y）。
- 标题右边有个小三角，**用来收放这一条的内容**（展开时朝下 ▽，收起时朝右 ▷）；默认展开。
- 展开后显示：
  - 笔记型：正文（最多 4 行，超出省略；服务端最多带 200 字）；
  - 清单型：每一项的内容和勾选状态（最多列前 6 项，多的用「… 还有 N 项」带过），
    并且可以直接在首页点一下勾掉 / 取消勾选，不用跳进备忘录页。
- 收放状态在勾选、刷新速览后保持不变。
- 点整条（小三角和勾选框以外的地方）跳到备忘录页并选中该条。
- 右上角「去备忘录页」跳转。

---

## 4. 每个模块的作用

### 4.1 首页
信息汇总与入口，让人 3 秒内掌握今天该干嘛，不用来回切页。

### 4.2 课程
管理「按教学周组织的周课表」。核心是每周一套课表、各周独立、可整周复制，能准确反映临时调课和不同周次的上课安排。

### 4.3 天气
提供所在地实时天气与未来预报，替代打开手机查天气这一步。联网失败要优雅降级。

### 4.4 备忘录
随手写东西的地方。两种形态：多行笔记，和可勾选的清单（购物、待办）。支持置顶、搜索、归档。

### 4.5 数据与设置
掌控自己的数据：看得见存在哪、能备份、能还原、能重置；以及调整学期、作息、城市、外观。

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 课程

数据（每节课一条）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 唯一标识 |
| week | int | 所属教学周（1-20） |
| day | int | 星期几（1=周一 … 7=周日） |
| slot | string | 节次 id，指向用户在设置里自定义的某一条节次（如 `p1`） |
| spanEnd | string | 可选，跨节次课（如连上两节）的结束节次 id |

> 节次本身不在课程模块里定义，全部由用户在「设置 → 作息与节次」中自行增删和命名。
> 首次启动节次列表为空，添加课程前会引导先去设置里建节次。
| name | string | 课程名 |
| location | string | 上课地点，自由文本，用户自填（如「A 楼 302」） |
| teacher | string | 老师，可空 |
| note | string | 备注，可空 |
| color | string | 可选，课程块色标 |

操作：
- 切换周次（下拉 1-20 周 / 上一周下一周 / 回到本周）。
- 新增课程（弹窗表单）。
- 编辑课程（点课块打开同一弹窗预填）。
- 删除课程（弹窗内删除，二次确认）。
- 整周复制：选源周 → 多选目标周 → 冲突策略三选一（覆盖 / 合并保留两边 / 只填空的周）。
- 清空某一周。
- 导入课表：见 5.6。

### 5.6 导入课表（从教务系统导出的 xlsx 直接生成课表）

教务系统导出的「学生课表」是一张「节次 × 星期」的网格，课程写在格子里，
格式为 `课程名称(考核方式)(课程类别)专业班级{周次范围 教师 教室}`，
一个格子里可能塞两门课（换行分隔），课程格通常按两节纵向合并。

流程：
1. 课程页点「导入课表」，选一个 `.xlsx`；
2. 服务端只解析、**不写盘**，返回预览：表格名/学期、读到多少条课程记录、覆盖哪些周、
   一共要排多少节课、会新建哪些节次、解析中跳过了什么（例如某格没写周次）；
3. 用户确认后再导入：先把当前数据自动备份一份 → 建好缺的节次 → 按每门课的周次范围
   把课铺到对应的每一周；
4. 导入方式二选一：追加（保留已有课程）或覆盖（导入涉及的周直接替换）。

规则与边界：
- 只支持 `.xlsx`（Excel 2007 以上）。`.xls` 请先另存为 `.xlsx`；读不动会给出中文提示，不报错、不动数据。
- 解析全部在内存里完成，上传的文件不落盘。
- 合并单元格的值即使被重复存了多份，也只算一次课。
- 节次按表格里的行序新建，**时间一律留空**——导入不替用户猜作息，填时间仍由用户自己做。
- 认不出表头（没有「节次」列或没有「星期一…星期日」）时明确报错，不做猜测。
- 单双周（`1-17周(单)`）、周次列表（`1,3,5周`）、周次范围（`4-17周`）都能认。

### 5.2 天气

数据：
- 当前实况：温度、体感、天气状况、湿度、风速风向、气压、紫外线、能见度、降水概率。
- 24 小时逐小时：温度、降水概率。
- 7 天预报：日期、天气、最高/最低、降水概率。
- 元信息：城市名、经纬度、更新时间、数据来源。

操作：
- 切换城市：天气页和设置页各有一个下拉，都读同一份「我的地点」列表，选中即切换。
- 添加地点：设置页「＋ 添加地点」展开搜索框，输入城市名 → 搜到后点结果即加入列表并设为当前。
- 移除地点：设置页「已保存」里点 ×；删掉的如果正是当前城市，自动顺位到剩下的第一个。
- 手动刷新。
- 设置页配置自动刷新间隔；也可以直接手填经纬度（同样会变成一条地点）。
- 失败降级：优先返回上一份缓存并标注时间，没有缓存则显示提示文案。

数据源：Open-Meteo（免注册、免 key）。经纬度由地点列表提供，WMO 天气码映射为中文描述与图标。

### 5.3 备忘录

数据：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 唯一标识 |
| title | string | 标题 |
| body | string | 正文，多行文本（type=text 时用） |
| type | string | `text` 笔记 / `todo` 清单 |
| items | array | 清单项 `[{text, done}]`（type=todo 时用） |
| pinned | bool | 是否置顶 |
| archived | bool | 是否归档 |
| createdAt / updatedAt | string | 创建 / 修改时间 |

操作：
- 新建（默认 text，可切 todo）。
- 编辑标题、正文、清单项（增删改勾）。
- 清单形态：勾选框 + 内容框 + 删除键，**每条一行**（一行一件事），内容框占满右侧剩余宽度；
  整块清单排在「清单」这个标签底下，左边对齐。「+ 加一行」在清单末尾。
- **保存**：编辑区右下有一枚「保存」键，点了立刻落盘；同时保留边打字边自动保存（停手 0.5 秒存一次），旁边显示「已保存 / 有改动…」。切到别的备忘录之前会先把还没存下的改动补存，绝不因为手快丢内容。
- 置顶 / 取消置顶、归档 / 还原、删除（删除二次确认）。
- 按关键词搜索（标题 + 正文 + 清单项）。
- 列表默认过滤 archived，可在「归档」分组查看。

列表的显示规则：
- 第一行是标题（无标题就写「无标题」，置顶的前面带 ★）。
- 第二行是「**类型 · 摘要**」：类型写「笔记」或「清单」，后面跟摘要。
- 不写「未分类」这类占位词——那会让人以为这条缺了什么，而它的类别本来就是「笔记」或「清单」。

### 5.4 数据与设置

数据（settings.json）：

| 字段 | 说明 |
| --- | --- |
| displayName | 首页问候用的称呼，**由用户填写，不预置** |
| semesterName | 学期名，用户自定义，如「2026-2027 第一学期」 |
| week1Monday | 第 1 周周一的日期（必填，用户录入，用于算教学周） |
| weekStartsOn | 每周起始日，默认周一 |
| periods | 节次列表，**初始为空**，每条含 id / 名称 / 开始时间 / 结束时间，用户自行增删改 |
| campus | 校区名，**初始为空**，用户自定义（可留空，仅用于课程备注展示） |
| weatherCity | 当前天气城市，**初始未设置**，首次使用天气页时引导选择或搜索 |
| weatherCities | 「我的地点」列表，**初始为空**，最多 20 条；每条含 id / 名称 / 省份 / 经纬度 / 时区，可添加、切换、移除 |
| refreshMinutes | 天气自动刷新间隔 |
| theme | 外观（浅色 / 深色 / 跟随系统） |

操作：改设置（即时保存）、管理节次列表（添加 / 重命名 / 改时间 / 删除 / 上下移动）、管理「我的地点」（搜索添加 / 下拉切换 / 移除）、打开数据目录、立即备份、选择备份还原、清空全部数据（强制先备份 + 二次确认）。

我的地点规则：

- 只由用户搜索或手填坐标产生，**不预置任何城市**。
- 添加后立即成为当前城市；坐标相同视为同一地点，只更新名称，不重复添加。
- 移除当前城市时自动顺位到列表里的第一个；列表清空则当前城市回到「未设置」。
- 天气页和设置页共用同一份列表，选城市统一走这套接口，不存在两处状态各写各的。

节次管理规则：
- 节次完全由用户定义：想分几节就几节，名称和起止时间随意（支持中午时段、晚课、实训等特殊安排）。
- 添加方式只有一个：「+ 添加节次」，点一下加一条空白节次，名称和时间自己填（不放批量生成的按钮，避免用户莫名其妙多出一堆节次）。
- 删除某节次时，若已有课程挂在上面，必须提示「有 N 节课使用了这个节次」，并让用户选择：改挂到别的节次 / 连同课程一起删除 / 取消。

> 说明：为遵守「零预置原则」，本产品不再内置任何学校作息时间表、也不预置示例课表。

---

## 6. 各个模块之间的关系

```
                 ┌──────────── 数据源 ────────────┐
                 │                                │
  settings ──────┤ 周次计算、作息时间、默认城市     │
                 │                                │
  courses  ──────┼──→ 教学周计算依赖 settings      │
                 │                                │
  notes    ──────┤                                │
                 │                                │
  weather  ←─────┴──→ 城市经纬度依赖 settings      │
                      外部 Open-Meteo 接口         │
                 └────────────────────────────────┘

   首页 = courses(今天) + weather(今天) + notes(最近3条)
```

依赖说明：
- **首页是只读聚合层**：不重复存数据，实时从课程、天气、备忘录三个源取当天切片。
- **课程依赖设置**：「今天是第几教学周」由 `week1Monday` 算出；节次起止时间在设置里维护，课程只存 `slot` 编码。
- **天气依赖设置**：默认城市与刷新频率来自设置，经纬度在服务端城市表里。
- **备忘录独立**：不依赖其他模块，只在首页被摘要引用。
- **改设置会牵动首页与课程**：改了第一周日期，所有周次判断立即变；改了作息，首页时间显示立即变。
- 模块间不互相写入，全部通过各自的 JSON 文件与服务接口读写。

---

## 7. 本地数据、备份与恢复需求

### 7.1 存储位置

```
D:\App\MyDay\
├── app\                 程序代码（后端服务 + 前端页面）
├── data\                数据目录（唯一真实来源）
│   ├── settings.json
│   ├── courses.json
│   ├── notes.json
│   ├── weather_cache.json
│   └── backups\
│       └── MyDay-backup-YYYYMMDD-HHMM.zip
├── design\              原型稿
├── PRD.md
└── 启动 MyDay.bat       双击启动
```

### 7.2 持久化规则

- 任何写操作立即落盘（改动后 300ms 内写入 JSON），不依赖浏览器 localStorage。
- 写入采用「先写临时文件 → 原子替换」，避免写入中断导致文件损坏。
- 首次启动自动生成四个数据文件的默认版本；文件损坏时保留损坏副本并回落到默认版本，界面给出提示。
- 天气结果缓存到 `weather_cache.json`，断网时首页仍可显示最后一次成功的天气，并标注「数据时间」。

### 7.3 备份与恢复

- 手动备份：设置页一键导出 zip，内含四个 JSON + 版本号 + 导出时间。
- 自动备份：每次清空数据前强制备份；可选开启每日首次启动时自动备份一份（保留最近 10 份）。
- 还原：选择 zip → 二次确认 → 覆盖现有数据 → 重启后生效。
- 数据目录可在设置页一键用资源管理器打开。
- 服务不监听外网，仅绑定 127.0.0.1。

---

## 8. 第一版必须完成的功能

全局
1. 双击启动脚本 → 本地服务起来 → 浏览器自动打开首页。
2. 侧边五宫格导航，切换不刷新页面。
3. 顶栏常显日期与第几教学周。
4. 所有写操作即时落盘，刷新 / 关机重启后数据还在。

课程
5. 周次切换（1-20 周）、回到本周。
6. 课程增删改。
7. 整周复制 + 三种冲突处理策略。
8. 本周课程数统计。
9. 导入课表：读教务系统导出的 xlsx，先预览再导入，按周次范围铺到各周。

天气
10. 实况（温度、体感、天气、湿度、风、气压、紫外线、能见度、降水概率）。
11. 未来 24 小时逐小时温度。
12. 未来 7 天预报。
13. 城市切换与手动刷新。
14. 「我的地点」：搜索添加、下拉切换、逐条移除，删当前城市自动顺位。
15. 断网 / 接口失败时降级显示，优先用缓存且标注时间。

备忘录
16. 新建、编辑、删除（删除二次确认）。
17. 笔记型与清单型两种形态，清单可勾选。
18. 置顶、搜索、归档。

设置
19. 称呼、学期名、学期第一周周一日期（必填）、每周起始日、校区名，全部自定义。
20. 节次列表自由增删改与上下移动排序；「+ 添加节次」点一下加一条空白节次。
21. 「我的地点」管理：搜索添加、下拉切换、逐条移除；默认城市未设置时有引导。
22. 天气刷新间隔、外观主题。
23. 打开数据目录、一键备份、选择文件还原、清空全部数据（含二次确认与强制备份）。

首页
24. 今日课程列表（含已上/正在上/下一节状态与空状态）。
25. 今日天气卡片（未设置城市与失败时均有引导/降级态）。
26. 备忘录最近 3 条速览，点击跳转并选中。

---

## 9. 第一版暂时不做的功能

明确推迟，但结构上留好扩展位：

| 不做 | 原因 |
| --- | --- |
| 登录 / 账号 / 多用户 | 单人本机使用，无必要 |
| 云同步、云数据库 | 与「数据只在本地」冲突 |
| 手机端 / 响应式适配 | 只在电脑浏览器用 |
| 部署上线、公网访问 | 同上，服务只绑 127.0.0.1 |
| 桌面弹窗提醒、上课闹钟、课程倒计时 | 第二版再加 |
| 作业、考勤、成绩、绩点统计 | 超出「日常总览」范围 |
| 图片上传、富文本排版、Markdown 渲染 | 备忘录只做纯文本与清单 |
| 导出 PDF / 打印 | 暂无需求 |
| 天气历史曲线、多城市同屏对比、空气质量图表 | 保留单城市 + 7 天已够用 |
| 自动抓取教务系统课表 | 凯森明确不希望再登录教务系统 |
| 单双周 / 起止周排课规则 | 已确认改用「每周一套 + 整周复制」方案替代 |

---

## 10. 可实际检查的验收标准

逐条打勾即为通过。

**启动与运行**
- [ ] 双击启动脚本，3 秒内浏览器自动打开首页，无需手动输地址。
- [ ] 关闭浏览器再打开，之前的数据全部还在。
- [ ] 重启电脑后再启动，数据一条不少（重点验收项）。
- [ ] 关掉服务后打开页面会给出「服务未运行」的提示，而不是白屏。

**持久化与文件**
- [ ] 在 `D:\App\MyDay\data` 里看到 settings / courses / notes / weather_cache 四个 JSON。
- [ ] 用记事本打开 courses.json，能看到刚添加的课，格式清晰可读。
- [ ] 清空浏览器缓存和数据后重开 App，课程与备忘录仍完好（证明不依赖 localStorage）。
- [ ] 手动改坏某个 JSON（比如删一个括号）后再启动，App 不崩溃，给出提示且能恢复使用。

**课程（全部由用户录入，不预置）**
- [ ] 全新安装后打开课程页：节次为空、课表为空，页面显示「先去设置里添加节次」的引导，不报错、不出现任何示例课。
- [ ] 在设置里添加两条节次后，课程表立刻出现对应两行；删除节次后行数同步变化。
- [ ] 设置页「+ 添加节次」点一下只加一条，点五下正好五条，列表当场就能看到；界面上没有「生成若干空白节次」这类批量按钮。
- [ ] 在设置里改某节次的起止时间，课程表和首页的时间显示跟着变。
- [ ] 删除一个已被课程占用的节次时，提示有几节课受影响，并给出「改挂到其他节次 / 一起删除 / 取消」三种处理。
- [ ] 在 A 周添加一节课，切到 B 周看不到它，切回 A 周还在。
- [ ] 任意两周可以是两套完全不同的课表，互不干扰。
- [ ] 把某周复制到另外三周，切过去三周课表完全一致；目标周已有课时三种策略行为正确。
- [ ] 点某个课块能改课程名、地点、节次，刷新后保持。
- [ ] 课程页「导入课表」选中教务系统导出的 xlsx 后，先看到预览（多少条课程、覆盖哪些周、会新建哪些节次），此时数据没有任何变化。
- [ ] 确认导入后，每门课按它自己的周次范围铺到对应周；两节连上的课占两个节次；一格两门课都进来。
- [ ] 导入方式选「追加」时已有课程保留，选「覆盖」时被涉及的周被替换、没涉及的周不动。
- [ ] 重复导入同一份文件不会产生重复节次（同名节次直接复用）。
- [ ] 导入一个不是 xlsx 的文件时给出中文提示，数据不受影响。
- [ ] 跨节次的课（占两个相邻节次）在表格里能连续占两行显示。
- [ ] 全 App（代码 + 数据文件）搜索不到任何真实课程名、教室编号、学校作息时间。

**首页**
- [ ] 今天的课按节次顺序排列，已结束的呈灰色。
- [ ] 首页备忘录速览里，清单型笔记能看到每一项的内容和勾没勾；直接在首页点勾选框能勾掉，完成数 x/y 同步变化，进备忘录页看到的也是最新的。
- [ ] 首页速览里笔记型能看到正文（长了会截断）；点标题右边的小三角能收放这一条，收起后只剩标题和摘要，勾选 / 刷新后收放状态不丢。
- [ ] 清单超过 6 项时首页只列前 6 项并提示「还有 N 项」，在首页勾选**不会**把后面的项弄丢。
- [ ] 点勾选框不会跳到备忘录页；点整条其它地方才会跳过去并选中。
- [ ] 今天是周五，首页就只显示周五的课，不多不少。
- [ ] 在设置里填任意一个第 1 周周一日期，首页算出的教学周数与手推结果一致（跨周边界当天必测）。
- [ ] 未设置节次时间时，今日课程区块显示空状态而不是错乱的时间。
- [ ] 备忘录改一条内容，回首页刷新，速览里看到的是最新那条。

**天气**
- [ ] 未设置城市时，天气页显示「选一个城市」的引导，不会默认展示某个具体城市。
- [ ] 选好城市后能看到实时温度，且和手机天气相差不超过 2 度。
- [ ] 能看到未来 7 天预报和 24 小时逐小时温度。
- [ ] 拔网线或断网后打开天气页：有缓存则显示上次数据并标注旧时间，无缓存则显示友好提示，页面其他部分照常可用。
- [ ] 改城市后，天气数据随之改变。
- [ ] 设置页「城市」是可用状态：点「＋ 添加地点」能搜到城市并加入列表，加入后立刻变成当前城市。
- [ ] 已保存的地点能逐条移除；删掉当前城市后自动切到列表里的第一个，全部删空后回到「未设置」引导。
- [ ] 重复添加同一个城市不会出现两条记录；重启软件后「我的地点」还在。

**备忘录**
- [ ] 新建清单型笔记，勾选两项后显示 2/4，刷新后勾选状态还在。
- [ ] 清单每条一行：左边勾选框、中间能直接打字的内容框（占满剩余宽度）、右边一个 ×；整块排在「清单」标签下面，不会被挤到中间或右边。
- [ ] 编辑区有「保存」键，点一下提示已保存；旁边状态在「有改动…」和「已保存」之间切换；不点也会自动保存。
- [ ] 敲完标题立刻点左边另一条，再点回来，刚敲的内容还在（切走前会补存）。
- [ ] 列表第二行显示「笔记 · 摘要」这类内容；不会出现「未分类」这类占位词。
- [ ] 置顶的笔记始终排在最前，首页速览里也优先出现。
- [ ] 归档的笔记不出现在「全部」列表，能在「归档」里找回并还原。
- [ ] 搜索关键词能同时命中标题、正文和清单项内容。

**备份与恢复**
- [ ] 点「立即备份」能在 D:\App\MyDay\data\backups 下生成带时间戳的 zip。
- [ ] 删掉全部数据后，用刚才的 zip 还原，课程、备忘录、设置全部回来，与备份前一致。
- [ ] 点「清空全部数据」时必须先二次确认，且过程中自动产生一份备份。
- [ ] 点「打开数据目录」能直接唤起资源管理器到 data 文件夹。

**非功能**
- [ ] 任意操作都不会出现浏览器原生报错弹窗。
- [ ] 页面首次加载（不含天气网络请求）在 1 秒内完成。
- [ ] 服务只监听 127.0.0.1，局域网其他电脑访问不到。
